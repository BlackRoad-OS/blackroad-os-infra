# ==============================================================================
# BlackRoad OS Infrastructure Environment Variables
# ==============================================================================
# Copy this file to .env and fill in the values.
# NEVER commit the .env file to git - it contains secrets!
# ==============================================================================

# ------------------------------------------------------------------------------
# Terraform Backend (S3)
# ------------------------------------------------------------------------------
TF_STATE_BUCKET=blackroad-tfstate
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# ------------------------------------------------------------------------------
# Provider Tokens
# ------------------------------------------------------------------------------
# Cloudflare API token with DNS edit permissions
CLOUDFLARE_API_TOKEN=

# Railway API token from https://railway.app/account/tokens
RAILWAY_TOKEN=

# GitHub Personal Access Token with repo and admin:org permissions
GITHUB_TOKEN=

# ------------------------------------------------------------------------------
# GitHub Configuration
# ------------------------------------------------------------------------------
GITHUB_OWNER=blackroad-os

# ------------------------------------------------------------------------------
# Railway Configuration
# ------------------------------------------------------------------------------
# Get project ID from Railway dashboard: Project → Settings → General → Project ID
RAILWAY_PROJECT_ID_DEV=
RAILWAY_PROJECT_ID_PROD=

# ------------------------------------------------------------------------------
# Common Service Environment Variables
# These are typically set in Railway, not locally
# Shown here for reference
# ------------------------------------------------------------------------------

# Database URLs (set in Railway, not local)
# DATABASE_URL=postgresql://user:password@host:5432/database
# REDIS_URL=redis://host:6379

# JWT Secrets (generate with: openssl rand -hex 32)
# JWT_SECRET=
# JWT_REFRESH_SECRET=

# External API Keys
# OPENAI_API_KEY=sk-...

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
# 1. For local development, only TF_STATE_BUCKET and provider tokens are needed
# 2. Service-specific secrets are managed in Railway directly
# 3. See docs/railway-troubleshooting.md for variable requirements per service
