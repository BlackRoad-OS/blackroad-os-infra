name: "🧭 Start Here (Workflow Chooser)"
description: "Pick what you're doing and we'll standardize it."
title: "🧭 "
labels: ["type:triage"]
body:
  - type: dropdown
    id: workflow
    attributes:
      label: "🚂 Choose your workflow"
      options:
        - "📡 Status Update"
        - "🐛 Bug"
        - "✨ Feature"
        - "🧹 Task / Chore"
        - "🧪 Spike / Experiment"
        - "🧠 RFC / Codex Proposal"
        - "🔥 Incident / SEV"
        - "🔐 Security"
        - "🚀 Release"
        - "🎨 Design Request"
        - "🧰 Tooling / Automation"
        - "🛰️ Infra / Ops"
    validations: { required: true }

  - type: dropdown
    id: status
    attributes:
      label: "🚦 Status"
      options: ["🟢 green", "🟡 yellow", "🔴 red", "⭕ unknown"]
    validations: { required: true }

  - type: dropdown
    id: priority
    attributes:
      label: "🎯 Priority"
      options: ["🟥 P0", "🟧 P1", "🟨 P2", "🟩 P3", "🟦 P4"]
    validations: { required: true }

  - type: dropdown
    id: progress
    attributes:
      label: "📈 Progress (0–7)"
      options: ["0","1","2","3","4","5","6","7"]
    validations: { required: true }

  - type: textarea
    id: next
    attributes:
      label: "👉 Next step"
      placeholder: "What should happen next? (link any issues/PRs)"
    validations: { required: true }
