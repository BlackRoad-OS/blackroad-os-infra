name: "🚀 Release"
description: "Ship it!"
title: "🚀 Release: "
labels: ["type:release"]
body:
  - type: dropdown
    id: type
    attributes:
      label: "🎯 Release Type"
      options: ["🟥 major (breaking)","🟧 minor (features)","🟩 patch (fixes)","🟦 hotfix"]
    validations: { required: true }

  - type: dropdown
    id: risk
    attributes:
      label: "🧨 Risk"
      options: ["🧊 low","🧨 medium","☢️ high","🪦 irreversible"]
    validations: { required: true }

  - type: textarea
    id: version
    attributes:
      label: "🏷️ Version / Tag"
      placeholder: "v1.2.3"
    validations: { required: true }

  - type: textarea
    id: whats_new
    attributes:
      label: "✨ What's new?"
      placeholder: |
        - New feature X
        - Fixed bug Y
        - Improved Z
    validations: { required: true }

  - type: textarea
    id: breaking
    attributes:
      label: "💥 Breaking changes (if any)"
      placeholder: "What needs to change for users?"
    validations: { required: false }

  - type: textarea
    id: rollout
    attributes:
      label: "🛰️ Rollout plan"
      placeholder: |
        Rollout:
        Monitoring:
        Rollback:
    validations: { required: true }

  - type: textarea
    id: validation
    attributes:
      label: "✅ Validation checklist"
      placeholder: |
        - [ ] Tests pass
        - [ ] Docs updated
        - [ ] Changelog updated
        - [ ] Tagged in git
    validations: { required: true }
